databases:
  - name: mysite_4jgk
    databaseName: mysite_4jgk
    user: mysite_4jgk_user
    plan: free

services:
  - type: web
    plan: free
    name: mysite
    env: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: mysite_4jgk
      - key: RAILS_MASTER_KEY
        sync: false